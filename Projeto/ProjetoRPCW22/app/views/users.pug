extends layout

block content
  div(class="w3-container w3-teal")
    h2.w3-left Lista de Utilizadores
    a(href="/" class="w3-button w3-green w3-right w3-margin-top") Voltar atrás
    a(href="/user/add" class="w3-button w3-right w3-green w3-margin-top w3-margin-right") Adicionar novo utilizador
  table(class="w3-table w3-bordered")
    tr
      th Identificador
      th Nome Completo
      th Passsword
      th Tipo
      th Eliminar
    each user in data
      tr
        td=user.id
        td=user.name
        td=user.pw
        td=user.type
        td
          a(href="/user/remove/" + user.id) X